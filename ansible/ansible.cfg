# ДЗ 6: Ansible — конфигурация
[defaults]
inventory = inventory/hosts.yml
remote_user = ubuntu
private_key_file = ~/.ssh/id_rsa
host_key_checking = False
roles_path = roles

[privilege_escalation]
become = True
become_method = sudo
